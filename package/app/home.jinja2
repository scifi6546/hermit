<!DOCTYPE html>
<head>
	 <link href="/static/index.css" rel="stylesheet">
</head>
<div class="topnav">
	<a href="{{LOGOUT_URL}}">Log Out</a>
	<a href="{{CONFIG_URL}}">Settings</a>
</div>
<div class="body">
{% for vid in videos %}
	<div class="video_div">
		<a href="{{vid.html_url}}">
			<img src="{{vid.thumb_url}}"/>
		</a>
		<a href="{{vid.html_url}}">{{vid.url}}</a>
	</div>
	{% endfor %}
</div>
